apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kafbat
spec:
  parentRefs:
    - name: dev-local
      namespace: default
      sectionName: https
  hostnames:
    - kafbat.dev.local
  rules:
    - backendRefs:
        - name: kafbat-ui-kafka-ui
          port: 80
